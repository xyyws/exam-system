<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.exam.system.mapper.SysClassMapper">

    <resultMap id="BaseResultMap" type="com.exam.system.entity.SysClass">
        <id column="id" property="id"/>
        <result column="class_name" property="className"/>
        <result column="grade_name" property="gradeName"/>
        <result column="dept_name" property="deptName"/>
        <result column="teacher_id" property="teacherId"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM sys_class WHERE id = #{id}
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT * FROM sys_class WHERE 1=1
        <if test="keyword != null and keyword != ''">
            AND class_name LIKE CONCAT('%', #{keyword}, '%')
        </if>
        <if test="status != null">AND status = #{status}</if>
        ORDER BY id DESC LIMIT #{offset}, #{limit}
    </select>

    <select id="countList" resultType="long">
        SELECT COUNT(*) FROM sys_class WHERE 1=1
        <if test="keyword != null and keyword != ''">
            AND class_name LIKE CONCAT('%', #{keyword}, '%')
        </if>
        <if test="status != null">AND status = #{status}</if>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_class (class_name, grade_name, dept_name, teacher_id, status, remark, create_by)
        VALUES (#{className}, #{gradeName}, #{deptName}, #{teacherId}, #{status}, #{remark}, #{createBy})
    </insert>

    <update id="update">
        UPDATE sys_class SET class_name = #{className}, grade_name = #{gradeName},
            dept_name = #{deptName}, teacher_id = #{teacherId}, status = #{status},
            remark = #{remark}, update_by = #{updateBy}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_class WHERE id = #{id}
    </delete>
</mapper>
